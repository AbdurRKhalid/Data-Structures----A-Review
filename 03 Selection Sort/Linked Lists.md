# Linked Lists
- With linked lists the elements can be anywhere in memory.
- Each item in linked list will hold address of next item in the list.
- A bunch of random memory addresses are linked together.
