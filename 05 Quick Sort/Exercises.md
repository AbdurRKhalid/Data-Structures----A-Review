# Exercises
4.5 ==> This will take around O(n) as we are touching each element at-least once.
4.6 ==> This will again take O(n) as we will be touching all elements in an array.
4.7 ==> This operation will take constant time so the Big O Notation will be O(1).
4.8 ==> This operation will take O(n^2) time as we are touching each element twice.
